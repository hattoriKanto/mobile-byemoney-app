import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },

  image: {
    width: '100%',
    resizeMode: 'contain',
  },
});

export default styles;
