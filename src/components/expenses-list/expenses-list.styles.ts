import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 20,
    flex: 1,
  },

  listContainer: {
    gap: 15,
  },

  notFoundText: {
    fontSize: 18,
    textAlign: 'center',
  },
});

export default styles;
