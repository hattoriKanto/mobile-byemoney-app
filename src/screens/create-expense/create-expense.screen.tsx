import {Text} from 'react-native';
import {AppContainer} from '../../ui';

export const CreateExpenseScreen = () => {
  return (
    <AppContainer>
      <Text>Create</Text>
    </AppContainer>
  );
};
